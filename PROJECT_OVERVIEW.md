# Date Him - AI虚拟男友应用

## 项目概述

这是一个Flutter应用，用户可以选择不同的AI虚拟男友进行约会互动。

## 🎨 设计特色

### 全屏沉浸式体验
- **无系统导航栏设计**：所有页面移除传统 AppBar，采用全屏沉浸式设计
- **悬浮按钮**：返回按钮和功能按钮都悬浮在页面上层
- **现代化交互**：Material Design 风格的涟漪点击效果

详细设计说明请查看：**`DESIGN_UPDATES.md`**

## 功能特性

### ✅ 已完成功能

#### 1. 首页（男友列表）
- 剧场主题背景
- 横向轮播展示男友卡片
- 每张卡片包含：
  - 男友图片
  - 名字
  - 描述
  - "Date him" 按钮
- 点击按钮进入角色设定页

#### 2. 角色设定页
- **全屏设计**：无系统导航栏
- **悬浮返回按钮**：左上角半透明圆形按钮

用户可以自定义以下属性：
- **给男友的称呼**：如"宝贝"、"亲爱的"等
- **性格喜好**：如"温柔体贴"、"霸道总裁"等
- **男友对用户的称呼**：如"宝宝"、"小可爱"等

设定完成后进入约会页面

#### 3. 约会页面（主交互页）

**全屏沉浸式设计**：
- **无系统导航栏**
- **悬浮返回按钮**：左上角半透明圆形按钮
- **悬浮功能按钮**：右上角垂直排列

**布局结构**：
- **上方区域**：角色展示（60%）
  - 优先显示视频（如果有）
  - 无视频则显示图片

- **悬浮功能按钮**（右上角）：
  - 💕 Date（约会）- 粉色
  - 🎵 Sing（唱歌）- 紫色
  - 📖 Diary（日记）- 蓝色
  - 🎁 Gift（礼物）- 橙色
  - 白色半透明圆形按钮，带阴影
  - *注：当前点击显示"功能开发中"*

- **下方聊天区**（40%）：
  - 消息列表（支持滚动）
  - 用户/AI消息气泡
  - 消息输入框
  - 发送按钮
  - 输入状态提示

### 🔧 待对接功能

以下功能UI已完成，需要对接后端API：

1. **获取男友列表**
   - 当前使用本地模拟数据
   - 需对接: `GET /boyfriends`

2. **AI聊天**
   - 当前返回固定模拟消息
   - 需对接: `POST /chat`

3. **角色设定保存**
   - 当前仅保存在内存中
   - 需对接: `POST /settings`

详细对接说明请查看 `API_INTEGRATION.md`

### 📋 功能待开发

右侧功能按钮的具体交互：
- Date：约会场景/剧情
- Sing：唱歌/音乐播放
- Diary：查看日记/聊天记录
- Gift：礼物系统

## 技术栈

- **Flutter**: 3.8.1+
- **Dart**: 3.8.1+

### 主要依赖包

```yaml
dependencies:
  provider: ^6.1.2           # 状态管理
  video_player: ^2.9.2       # 视频播放
  carousel_slider: ^5.0.0    # 轮播图
  shared_preferences: ^2.3.3 # 本地存储
  dio: ^5.7.0                # HTTP请求
  http: ^1.2.2               # HTTP请求
```

## 项目结构

```
datehim_flutter/
├── assets/                 # 资源文件
│   ├── images/            # 图片资源
│   ├── videos/            # 视频资源
│   └── icons/             # 图标资源
├── lib/
│   ├── main.dart          # 应用入口
│   ├── models/            # 数据模型
│   │   ├── boyfriend.dart
│   │   ├── character_settings.dart
│   │   └── chat_message.dart
│   ├── pages/             # 页面
│   │   ├── home_page.dart
│   │   ├── character_settings_page.dart
│   │   └── dating_page.dart
│   ├── services/          # 服务层
│   │   └── api_service.dart
│   └── widgets/           # 自定义组件
├── API_INTEGRATION.md     # API对接文档
├── PROJECT_OVERVIEW.md    # 项目概览（本文件）
└── pubspec.yaml           # 项目配置
```

## 快速开始

### 1. 安装依赖

```bash
flutter pub get
```

### 2. 运行项目

```bash
# iOS模拟器
flutter run

# Android模拟器
flutter run

# Chrome浏览器
flutter run -d chrome
```

### 3. 对接API

参考 `API_INTEGRATION.md` 文档进行API对接

## 设计特点

### 视觉设计
- **主题色**：粉色系（romantic风格）
- **首页**：剧场背景，营造浪漫氛围
- **卡片设计**：圆角卡片，渐变效果
- **聊天界面**：现代化消息气泡设计

### 交互设计
- **流畅导航**：首页 → 设定页 → 约会页
- **轮播效果**：平滑的横向滑动
- **实时反馈**：
  - 消息发送即时响应
  - AI输入状态提示
  - 按钮点击反馈

### 响应式设计
- 支持不同屏幕尺寸
- 视频/图片自适应显示
- 聊天区域自动滚动

## 下一步计划

1. ✅ 完成API对接
2. 实现右侧功能按钮具体交互
3. 添加聊天历史持久化
4. 优化视频加载性能
5. 添加用户认证系统
6. 实现消息推送功能

## 注意事项

1. **资源文件**：将图片、视频等资源放入对应的 `assets/` 文件夹
2. **API配置**：修改 `lib/services/api_service.dart` 中的 `baseUrl`
3. **平台支持**：当前支持 iOS、Android、Web
4. **视频格式**：确保视频格式兼容（推荐 MP4）

## 开发环境要求

- Flutter SDK: >= 3.8.1
- Dart SDK: >= 3.8.1
- iOS: >= 12.0
- Android: >= API 21 (Android 5.0)
