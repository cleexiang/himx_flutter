# 资源文件使用指南

## 📂 资源文件目录结构

```
assets/
├── images/          # 图片资源
│   ├── bg_home.jpg  # 首页背景图（必需）
│   └── ...
├── videos/          # 视频资源
│   └── ...
└── icons/           # 图标资源
    └── ...
```

---

## 🖼️ 必需的图片资源

### 1. 首页背景图

**文件名**: `bg_home.jpg`
**路径**: `assets/images/bg_home.jpg`
**用途**: 首页剧场背景
**推荐尺寸**: 1920x1080 或更高
**格式**: JPG/PNG
**代码位置**: `lib/pages/home_page.dart:41`

**添加方法**:
1. 将您的背景图片重命名为 `bg_home.jpg`
2. 放入 `assets/images/` 目录
3. 运行 `flutter pub get` 重新加载资源

---

## 📝 如何添加资源文件

### 步骤 1: 准备资源文件
将图片、视频等资源文件放入对应的文件夹：
- 图片 → `assets/images/`
- 视频 → `assets/videos/`
- 图标 → `assets/icons/`

### 步骤 2: 确认 pubspec.yaml 配置
资源路径已在 `pubspec.yaml` 中配置：

```yaml
flutter:
  assets:
    - assets/images/
    - assets/videos/
    - assets/icons/
```

### 步骤 3: 重新加载资源
```bash
flutter pub get
```

### 步骤 4: 在代码中使用

#### 使用本地图片：
```dart
Image.asset('assets/images/your_image.jpg')
```

#### 使用本地视频：
```dart
VideoPlayerController.asset('assets/videos/your_video.mp4')
```

---

## 🎨 推荐的资源规格

### 首页背景图 (bg_home.jpg)
- **尺寸**: 1920x1080 或 2560x1440
- **格式**: JPG（体积小）或 PNG（质量高）
- **风格**: 剧场主题、浪漫氛围
- **色调**: 温暖、柔和

### 男友头像
- **尺寸**: 400x600 或更高（竖版）
- **格式**: JPG/PNG
- **建议**: 使用高质量人物照片

### 男友展示视频
- **格式**: MP4（推荐）
- **编码**: H.264
- **分辨率**: 720p 或 1080p
- **时长**: 5-30秒（循环播放）

### 功能按钮图标
- **尺寸**: 128x128 或 256x256
- **格式**: PNG（透明背景）
- **风格**: 扁平化设计

---

## 🚨 注意事项

### 1. 文件大小
- 图片尽量压缩，避免应用体积过大
- 建议单张图片不超过 500KB
- 视频建议不超过 5MB

### 2. 文件命名
- 使用小写字母和下划线
- 避免使用空格和特殊字符
- 例如：`bg_home.jpg` ✅  `背景图片.jpg` ❌

### 3. 图片格式选择
- **JPG**: 适合照片、背景图（体积小）
- **PNG**: 适合需要透明背景的图标

### 4. 热重载
添加新资源后：
1. 运行 `flutter pub get`
2. 可能需要完全重启应用（不是热重载）

---

## 📋 当前已配置的资源

### 首页 (HomePage)
- ✅ 背景图：`assets/images/bg_home.jpg`

### 角色列表（模拟数据）
当前使用占位图片 URL，后续可以替换为本地资源：
```dart
// 示例：替换为本地图片
imageUrl: 'assets/images/boyfriend_1.jpg',
```

---

## 🔄 网络图片 vs 本地图片

### 网络图片
```dart
Image.network('https://example.com/image.jpg')
```
**优点**: 不占应用体积
**缺点**: 需要网络，加载慢

### 本地图片
```dart
Image.asset('assets/images/image.jpg')
```
**优点**: 加载快，无需网络
**缺点**: 增加应用体积

### 建议
- **固定资源**（如背景、图标）→ 使用本地图片
- **动态内容**（如用户头像、男友列表）→ 使用网络图片

---

## 📦 资源优化建议

### 1. 图片压缩工具
- [TinyPNG](https://tinypng.com/) - 在线压缩
- [ImageOptim](https://imageoptim.com/) - Mac 应用
- [Squoosh](https://squoosh.app/) - Google 工具

### 2. 多分辨率适配
可以为不同屏幕密度提供不同分辨率的图片：
```
assets/images/
  bg_home.jpg      # 1x
  2.0x/
    bg_home.jpg    # 2x
  3.0x/
    bg_home.jpg    # 3x
```

### 3. WebP 格式
考虑使用 WebP 格式，体积更小：
```dart
Image.asset('assets/images/bg_home.webp')
```

---

## ✅ 快速检查清单

- [ ] `bg_home.jpg` 已放入 `assets/images/` 目录
- [ ] 运行 `flutter pub get` 重新加载资源
- [ ] 图片尺寸合适（1920x1080 或更高）
- [ ] 图片已压缩，体积合理
- [ ] 完全重启应用查看效果

---

如有任何资源加载问题，请检查：
1. 文件路径是否正确
2. `pubspec.yaml` 中是否正确配置
3. 是否运行了 `flutter pub get`
4. 是否完全重启了应用
